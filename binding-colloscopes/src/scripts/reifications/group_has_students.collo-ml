pub let group_has_students(group_list: GroupList, group: Group) -> Constraint =
    sum student in group_list.students {
        $StudentInGroup(student, group_list, group)
    } + |group.prefilled_students| >== 1;
