import "collomatique" as collomatique;
import "collomatique_db" as *;

pub let constraint(_db: Db) -> Constraint = collomatique::default_constraint();
pub let objective(_db: Db) -> [LinExpr | Constraint] = collomatique::default_objective();
