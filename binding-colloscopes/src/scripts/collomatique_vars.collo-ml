import "collomatique_db" as *;
import "collomatique_types" as *;

query group_list_for_interrogation(db: Db, slot: SlotId, week: GlobalWeek) -> ?GroupListId = "
WITH ordered_weeks AS (
    SELECT
        pw.period_id,
        pw.week_index,
        pw.has_interrogations,
        (
            SELECT COUNT(*)
            FROM period_weeks pw2
            JOIN periods p2 ON p2.id = pw2.period_id
            JOIN periods p ON p.id = pw.period_id
            WHERE (p2.position < p.position)
               OR (p2.position = p.position AND pw2.week_index < pw.week_index)
        ) AS global_week_number
    FROM period_weeks pw
),
slot_info AS (
    SELECT s.subject_id, s.week_pattern_id
    FROM slots s
    WHERE s.id = ?
),
target_week AS (
    SELECT period_id, week_index, has_interrogations
    FROM ordered_weeks
    WHERE global_week_number = ?
),
week_is_disabled AS (
    SELECT 1
    FROM week_pattern_disabled_weeks wpd
    JOIN target_week tw ON tw.period_id = wpd.period_id AND tw.week_index = wpd.week_index
    JOIN slot_info si ON si.week_pattern_id = wpd.week_pattern_id
)
SELECT glsa.group_list_id AS id
FROM target_week tw
JOIN slot_info si
JOIN group_list_subject_associations glsa
    ON glsa.period_id = tw.period_id
   AND glsa.subject_id = si.subject_id
WHERE tw.has_interrogations = 1
  AND NOT EXISTS (SELECT 1 FROM week_is_disabled);
";

query groups_for_group_list(db: Db, group_list: GroupListId) -> [GroupNum] =
    "SELECT group_index FROM group_list_group_names WHERE group_list_id = ?";

let groups_for_interrogation(db: Db, slot: SlotId, week: GlobalWeek) -> [GroupNum] =
    match group_list_for_interrogation(db, slot, week) {
        gl as GroupListId {
            groups_for_group_list(db, gl)
        }
        _ {
            []
        }
    };

let count_groups_in_interrogation(db: Db, slot: SlotId, week: GlobalWeek) -> LinExpr =
    sum group in groups_for_interrogation(db, slot, week) {
        $GroupInInterrogation(Int(slot), Int(week), Int(group))
    };

let interrogation_has_groups(db: Db, slot: SlotId, week: GlobalWeek) -> Constraint =
    count_groups_in_interrogation(db, slot, week) >== 1;

pub reify interrogation_has_groups as $InterrogationHasGroups;

let student_in_group(student: StudentId, group_list: GroupListId, group: GroupNum) -> Constraint =
    $StudentGroup(Int(group_list), Int(student)) === Int(group);

pub reify student_in_group as $StudentInGroup;

query group_list_filling(db: Db, group_list: GroupListId) -> ?String =
    "SELECT filling_type FROM group_lists WHERE id = ?";

let group_list_is_prefilled(db: Db, group_list: GroupListId) -> Bool =
    group_list_filling(db, group_list) == "prefilled";

query group_prefilled_student_count(db: Db, group_list: GroupListId, group: GroupNum) -> ?Int =
    "SELECT COUNT(*) AS student_count FROM prefilled_group_students WHERE group_list_id = ? AND group_index = ?";

query students_for_automatic_group_list(db: Db, group_list: GroupListId) -> [StudentId] =
    "SELECT id FROM students EXCEPT SELECT student_id AS id FROM automatic_group_excluded_students WHERE group_list_id = ?";

let group_has_students(db: Db, group_list: GroupListId, group: GroupNum) -> Constraint =
    if group_list_is_prefilled(db, group_list) {
        (group_prefilled_student_count(db, group_list, group) ?? 0) >== 1
    } else {
        sum student in students_for_automatic_group_list(db, group_list) {
            $StudentInGroup(student, group_list, group)
        } >== 1
    };

pub reify group_has_students as $GroupHasStudents;

let student_at_interrogation_in_group(student: StudentId, slot: SlotId, week: GlobalWeek, group_list: GroupListId, group: GroupNum) -> Constraint =
    // Use inequalities to reduce the amount of helper variables
    $StudentInGroup(student, group_list, group) >== 1 and $GroupInInterrogation(Int(slot), Int(week), Int(group)) >== 1;

pub reify student_at_interrogation_in_group as $StudentAtInterrogationInGroup;

query student_prefilled_group(db: Db, student: StudentId, group_list: GroupListId) -> ?GroupNum =
    "SELECT group_index FROM prefilled_group_students WHERE student_id = ? AND group_list_id = ?";

let student_at_interrogation(db: Db, student: StudentId, slot: SlotId, week: GlobalWeek) -> Constraint =
    match group_list_for_interrogation(db, slot, week) {
        gl as GroupListId {
            if group_list_is_prefilled(db, gl) {
                $GroupInInterrogation(
                    Int(slot), Int(week), Int(student_prefilled_group(db, student, gl) ?? GroupNum(-1))
                )
            } else {
                sum group in groups_for_group_list(db, gl) {
                    $StudentAtInterrogationInGroup(student, slot, week, gl, group)
                }
            }
        }
        _ as None {
            0
        }
    } >== 1; // Test for bigger than one: we can't actually exceed it but this will save some reification constraints

pub reify student_at_interrogation as $StudentAtInterrogation;

