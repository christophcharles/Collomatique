# TODO: Revamp the CLI flags

Entirely rework the CLI command flags. In particular, `debug` should be
discarded (or drastically simplified) in favour of a simple way to run Python
commands/scripts against files.
