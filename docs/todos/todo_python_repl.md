# TODO: Python REPL inside the app

It might be useful to offer a Python REPL that drops the user straight into the
interpreter shipped with the application, with the current file already open.

It would live in the "Outils avancés" panel (`colloscopes/gtk4/src/editor/advanced_tools.rs`),
next to the other developer-facing tools.

This is most useful together with [the Python documentation](todo_python_doc.md):
the REPL is where one tries out what the documentation describes.
