# FIXME: Merging periods erases colloscope data

Merging two periods badly erases colloscope data — even when the group lists
are identical in both periods, in which case the colloscope data should be
preserved.
