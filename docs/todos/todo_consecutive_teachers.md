# TODO: Add soft objecctive to avoid same consecutive teachers

A hard option exists. A soft option should always be applied (maybe with an autodetection for subjects with a single teacher).
It should at least work for exactly periodic and once in a block of weeks model.

For other periodicities, to see. Might be more consuming, might not be worth it.
