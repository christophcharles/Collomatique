# ---> Rust
# Compiled files
*.o
*.so
*.rlib
*.dll

# Executables
*.exe

# Generated by Cargo
/target/

# ---> Nix
/result

# Written by `nix build`, and deliberately not committed: the flake is not how
# this repository is normally built -- default.nix is -- so a lock file in here
# would be stale nearly all of the time, and a stale pin is worse than none.
/flake.lock

# ---> Vscode
/.vscode
